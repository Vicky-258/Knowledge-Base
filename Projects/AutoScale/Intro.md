A simulation-based intelligent autoscaling system that forecasts future load using ML and scales resources proactively instead of reactively, with an integrated MLOps pipeline to continuously retrain and improve the model over time.

